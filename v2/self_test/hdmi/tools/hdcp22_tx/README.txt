In order to compile the driver and app for HDCP22 accordign to the IPK:
1. copy the app file hdcp_tx.c to esm_hdmi_sdk/esm_host_lib/samples/hdcp_tx/,
2. compile the driver and app by running the script compile_snps.sh at esm_hdmi_sdk/esm_host_lib/ folder.
